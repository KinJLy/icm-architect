# Ledger

The single running record of all reconciled transactions. Appended only by `02_Reconcile` once a period's discrepancies are resolved — see `CONTEXT.md`. Never hand-edit a posted row; add a correcting entry instead.

**Seed note:** if there's an existing spreadsheet ledger, import its rows below (oldest first) before the first live reconciliation run, so the running balance carries forward correctly. Until then this table is empty.

| Date | Description | Category | Amount | Running Balance | Source | Reconciled Period |
|---|---|---|---|---|---|---|
| | | | | | | |
